{
  "compilerOptions": {
    "target": "es5",                           // Elige la versión de ECMAScript que quieres compilar.
    "lib": ["dom", "dom.iterable", "esnext"],  // Especifica las bibliotecas estándar que TypeScript incluye automáticamente.
    "allowJs": true,                           // Permite la importación y el uso de archivos JavaScript.
    "skipLibCheck": true,                      // Omite la verificación de tipos en archivos de declaración.
    "esModuleInterop": true,                   // Habilita la inter-operabilidad entre módulos ES y CommonJS.
    "allowSyntheticDefaultImports": true,      // Permite importar módulos con un import por defecto incluso si no tienen un export default.
    "strict": true,                            // Habilita todas las opciones de verificación estricta.
    "forceConsistentCasingInFileNames": true,  // Asegura que el uso de mayúsculas y minúsculas en los nombres de archivo sea consistente.
    "noFallthroughCasesInSwitch": true,        // Evita que los casos de un switch caigan uno sobre otro sin un break.
    "module": "esnext",                        // Especifica el módulo que el compilador generará.
    "moduleResolution": "node",                // Habilita la resolución de módulos estilo Node.js.
    "resolveJsonModule": true,                 // Permite la importación de archivos JSON como módulos.
    "isolatedModules": true,                   // Asegura que cada archivo sea tratado como un módulo independiente.
    "jsx": "react-jsx"                         // Especifica la fábrica de JSX para React 17 o superior.
  },
  "include": ["src"]                           // Indica que solo los archivos dentro de "src" se compilarán.
}
